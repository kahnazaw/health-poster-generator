## Packages
html2canvas | For converting the DOM elements (poster) to canvas for PDF generation
jspdf | For generating the final PDF file from the canvas
framer-motion | For smooth animations and transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Cairo", "sans-serif"],
  display: ["Tajawal", "sans-serif"],
}
